var containsDuplicate = function(nums) {
  let numArr = []
  for (const num of nums ){
      if (numArr.includes(num)){return true}
      else {
          numArr.push(num)
      }
  }
    return false
};
